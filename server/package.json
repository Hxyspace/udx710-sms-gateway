{
  "name": "udx710-sms-gateway-notify-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PC-side SMS notify server for UDX710 SMS Gateway",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
